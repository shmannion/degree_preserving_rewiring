[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "degree-preserving-rewiring"
version = "1.0.0"
description = "Degree-preserving graph rewiring algorithms"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]

